schema_version = "1.0.0"

id = "omp_contact_solver"
version = "0.5.0"
name = "OMP Contact Solver"
tagline = "Bake CPU OpenMP cloth against animated collision meshes"
maintainer = "ysk424"
type = "add-on"

website = "https://github.com/ysk424/omp-contact-solver"
blender_version_min = "4.2.0"
platforms = ["windows-x64"]
license = ["SPDX:MIT"]

[permissions]
files = "Load the bundled native solver DLL"

[build]
paths_exclude_pattern = [
  "__pycache__/",
  ".*",
  "*.zip",
]
